# Documentation-only placeholders; this app does not auto-load .env.
# Core local features do not need credentials.
# Configure the optional Advisor in the app settings UI.
# Never commit the local database, real keys or exported personal settings.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=
